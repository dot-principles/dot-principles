# principles

The principle catalog. Each subdirectory is a namespace containing individual principle files, a `catalog.yaml` registry, and LLM context files used by `/dot-audit`, `/dot-prime`, and `/dot-scout`.

See [`DESIGN.md`](../DESIGN.md) §2 for the catalog structure spec.
