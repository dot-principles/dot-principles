# tests

Regression tests for `.principles` tooling. Run `bash tests/check-audit-gates.sh` and `bash tests/check-retired-prime.sh` before pushing; CI runs both via `.github/workflows/audit-gates.yml`.
