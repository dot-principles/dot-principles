# templates/copilot-cli

Template files used by `install.sh` to generate the GitHub Copilot CLI skill integration. `manifest.cfg` lists the output targets; `wrapper.md` is the command wrapper template.
