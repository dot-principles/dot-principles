# groups

Principle group definitions. Each `.yaml` file defines a named group that maps to a curated list of principle IDs. Reference a group in `.principles` with `@<name>` (filename without `.yaml`). 53 built-in groups covering languages, frameworks, design patterns, and architectural styles.
