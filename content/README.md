# content

This directory is the VitePress source tree for the public `.principles` documentation site.

- `index.md` is the landing page.
- The other Markdown files map the visitor journey from `why` to `getting started`, `how it works`, `commands`, `extending`, and `examples`.
- Canonical deep references remain in the repo root docs and are linked from the site rather than duplicated wholesale.