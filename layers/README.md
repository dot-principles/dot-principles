# layers

Artifact-type stack definitions. Each subdirectory is a stack; each stack has up to three layer files that define which principles activate automatically based on artifact type and detected context. See [`artifact-types.yaml`](artifact-types.yaml) for the master type registry.
