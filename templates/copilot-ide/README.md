# templates/copilot-ide

Template files used by `install.sh` to generate the GitHub Copilot IDE prompt integration. `manifest.cfg` lists the output targets; `wrapper.md` is the command wrapper template.
