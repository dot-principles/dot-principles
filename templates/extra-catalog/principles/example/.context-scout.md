# .principles scout context — example
# Used by dot-scout Phase 1.2 to extend artifact detection.
# Add one ## Detection rule block per detection rule in this namespace.

## Detection rule: <rule-name>

<Description of what files or directories this rule matches.>

When matched:
- Artifact type: `<artifact-type>`
- Suggest group: `@<group-name>`

# Add one ## Detection rule block per rule. Remove this file if your namespace
# does not need custom detection (most namespaces won't need it).
